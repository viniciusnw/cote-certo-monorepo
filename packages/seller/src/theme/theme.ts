import { materialLightTheme } from './materialLightTheme';

export type MaterialColor =
  | 'primary'
  | 'secondary'
  | 'error'
  | 'warning'
  | 'info'
  | 'success';

export const theme = {
  light: materialLightTheme,
};
